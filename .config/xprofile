#!/bin/sh
source "$HOME/.config/screenlayout/desktop-pc.sh" # Load screenlayout script (you WONT need this, only for MY MONITOR SETUP)
xset r rate 300 50 & # Increase xrate (trust me, you WANT this)
xset s off
xcompmgr &
dunst &
setbg &
dwmbar &
m2c -m 0 & # Stream primary monitor to a virt cam
